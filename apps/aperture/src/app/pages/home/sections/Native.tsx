import { ButtonMain } from "@portfolio/aperture";

export default function Native() {
	return (
		<div>
			<div className="container flex-col gap-[3.75rem]">
				<div>
					<ButtonMain className="mt-5">Check it out</ButtonMain>
				</div>

			</div>
		</div>

	);
}